# go-http-server

This application responds with **'Hello World'** to any HTTP request.

## Usage

1. Run the server on port 8080:
```bash
go run .
```

2. Open a new terminal and run:
```bash
curl -X GET "http://localhost:8080"
```

3. The server will respond with 'Hello World'.
